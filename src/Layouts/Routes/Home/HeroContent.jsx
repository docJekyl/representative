import cx from 'clsx';
import { Title, Text, Container, Button, Overlay, Image } from '@mantine/core';

import logo from '../../../assets/logo.png'

export default () => {
  return (
    <div>
      <Image src={logo} />
    </div>
  );
}